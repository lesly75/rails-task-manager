<h1>All Tasks</h1>

<% @tasks.each do |task| %>
<li><%= link_to task.title, task_show_path(task) %></li>
 <% end %>
</ul>
<%= form_with model: @restaurant do |f| %>
<p>Add new tasks
  <%= f.text_field :name %>
 <%= f.submit %>
<% end %>
